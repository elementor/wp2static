name: CI

on: [push]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
    - uses: actions/checkout@v1
    - uses: php-actions/composer@master
    - name: Composer test
        with:
          command: test
